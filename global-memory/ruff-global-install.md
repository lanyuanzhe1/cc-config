---
name: ruff-global-install
description: ruff 0.12.0 is installed globally via Anaconda — no project config needed
metadata: 
  node_type: memory
  type: reference
  originSessionId: a2807aa9-74c7-466e-ac86-21a9fa87579f
---

ruff 0.12.0 is globally installed at `A:\Software\Anaconda\Scripts\ruff.exe` (Windows) and works without any project-level config file. It found 20 issues across the classical HNN codebase using built-in defaults. Do not suggest installing or configuring ruff — it is already available. A project-level `[tool.ruff]` section in `pyproject.toml` could be added if custom rule selection is desired, but is not required.
